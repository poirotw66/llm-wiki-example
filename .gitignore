**/.DS_Store
**/.obsidian/
wiki/.obsidian/
# Cursor local skills copy — canonical source is skills/ (sync locally or via npx)
.cursor/
# uv local environment
.venv/
__pycache__/
# Docling / RapidOCR model weights (download locally; ~100MB+)
models/docling/
