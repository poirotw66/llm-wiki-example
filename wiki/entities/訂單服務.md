---
title: "訂單服務"
type: entity
status: active
updated: "2026-06-17"
source_count: 1
tags: ["訂單", "後端", "範例"]
description: "【範例】虛構 order-service：訂單 CRUD、JWT 閘道後之 REST API 與速率限制。"
resource: "訂單-api-簡介"
timestamp: "2026-06-17T00:00:00Z"
---

# 訂單服務

> **【範例】** 服務名稱、URL、團隊均為虛構；僅示範 `entity` 頁應如何盤點系統與引用來源。

## Summary

**訂單服務**（order-service）為交易平台後端團隊維護的訂單領域服務，負責建立、查詢、列表與取消訂單；對外經 API Gateway 暴露 **REST API**（`/v1`）。（確定）[訂單 API 簡介](../sources/訂單-api-簡介.md)

## Key Points

| 項目 | 說明 |
|------|------|
| 擁有者 | 交易平台後端（虛構） |
| 對外 Base URL | `https://api.example.internal/v1`（範例） |
| 認證 | Bearer JWT（SSO 發行） |
| 文件版本 | 內部簡介稿（虛構）；完整規格指向「訂單服務 API v2」 |

**主要端點**（確定）[訂單 API 簡介](../sources/訂單-api-簡介.md)：

| 方法 | 路徑 | 用途 |
|------|------|------|
| POST | `/orders` | 建立訂單（201，支援 `idempotency_key`） |
| GET | `/orders/{order_id}` | 查詢單筆 |
| GET | `/orders` | 列表（`status`、`created_after`） |
| POST | `/orders/{order_id}/cancel` | 取消（僅 `PENDING`） |

**營運**（確定）[訂單 API 簡介](../sources/訂單-api-簡介.md)：

- 速率限制：600 req/min／API Key；429 + `Retry-After`
- 錯誤追蹤：回應含 `request_id` 供客服／維運對照

## Evidence

- API 行為、認證、端點與錯誤格式均來自內部簡介稿歸檔。[訂單 API 簡介](../sources/訂單-api-簡介.md)

## Relationships

- related_to: [REST API](../concepts/rest-api.md)
- used_in: [訂單 API 簡介](../sources/訂單-api-簡介.md)

## Open Questions

- 與庫存服務、金流服務的同步邊界（稿內未描述）。（未知）
- Webhook `order.shipped` 是否已上線。（未知）[訂單 API 簡介](../sources/訂單-api-簡介.md)
